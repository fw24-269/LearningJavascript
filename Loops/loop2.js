let factorial = 1;

// Using a while loop to calculate the factorial
let i = number;
while (i > 0) {
    factorial *= i;
    i--;
}

// Print the result
console.log("The factorial of " + number + " is: " + factorial);